"""
3i Fund Portal — Participation-ELOC Templates package.

Named, company-specific Purchase Notice / Purchase Confirmation templates whose fields
are chosen from the DealTermsServer field catalog, plus a separate
(company, pricing_period, document_type) -> template mapping.

Isolated from the legacy `purchase_notices` template code — nothing here touches the
existing collections or endpoints.
"""
